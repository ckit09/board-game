# Mahjong Server Environment Variables

# Port (auto-assigned by deployment platforms, default 3000)
PORT=3000

# Node environment
NODE_ENV=production

# Frontend URL for CORS (optional, defaults to '*' if not set)
# Set this to your GitHub Pages URL in production
FRONTEND_URL=https://yourusername.github.io

# Example values:
# FRONTEND_URL=http://localhost:8000 (local development)
# FRONTEND_URL=https://yourusername.github.io (GitHub Pages)
# FRONTEND_URL=* (allow all origins - development only)
